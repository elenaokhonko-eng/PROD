# Harbor contract-to-test traceability

Status: preparation only. These rows describe intended gates, not release-candidate results. `Prepared vector` means the deterministic input and expected invariant exist but still need an adapter to Backend's final integrated symbol or migration.

| Requirement | Test / gate | Layer | Fixture or input | Expected result | Evidence artifact | Owner | Preparation status |
|---|---|---|---|---|---|---|---|
| Exact candidate identity | `resolveRunContext` in every Playwright lane | Harness | `HARBOR_RELEASE_SHA`; checked-out Git SHA | Full SHA matches HEAD; live lanes fail closed on mismatch | Per-lane evidence JSON and HTML metadata | Test | Implemented |
| Preview target isolation | `requirePreviewBaseUrl`; `requirePreviewSupabaseUrl`; authenticated production route guard | Harness/browser | Exact preview app/Supabase hosts; explicit production denylists | Non-HTTPS, wrong-host, production target or production redirect fails closed before evidence is claimed | Guard unit output and browser evidence JSON | Test + Architect | Implemented; deterministic guards passed |
| Two-user ownership/RLS | `both Clerk sessions expose their seeded supabase_uuid claim`; `two users can read only their own case`; `pattern-c-rls.test.sql` | Browser/API + local DB | User A/B Clerk states and seeded cases | Each user sees only its case | Auth evidence JSON; pgTAP TAP output | Test + Backend | Implemented; not run |
| Clerk Pattern C claim | `both Clerk sessions expose their seeded supabase_uuid claim` | Auth/browser | User A/B `supabase` JWT template | Claim equals fixture UUID; JWT `sub` is not used as owner | Auth trace and evidence JSON | Test + Architect | Implemented; not run |
| Cross-user and zero-row writes | `a cross-user zero-row write cannot change the owner-visible value`; pgTAP update assertions | API + local DB | Protected owner-A case and original server value | 403/404 or zero changed rows; value unchanged | API trace; TAP output | Test + Backend | Implemented; not run |
| Service-role path authorization | `missing or invalid worker authorization never enters service-role paths` | API | Protected edge paths; absent/invalid worker secret | 401 with no redirect or mutation | Auth evidence JSON | Test + Backend | Implemented; not run |
| Onboarding bootstrap | `anonymous narrative is handed to the signed-in bootstrap exactly once` | Synthetic browser | Fixed anonymous narrative | One bootstrap request; pending narrative cleared only after success | Synthetic trace | Test + Frontend | Implemented synthetic; live bootstrap still open |
| Evidence file validation/upload | Slice 5 upload; `evidence upload validation` | Browser/API/storage | Tiny valid PNG; unsupported MIME/extension; MIME/magic-byte mismatch; file above configured storage limit | Invalid files fail with a validation 4xx and create no object/row; valid file registers once | Trace, screenshot, storage/DB query | Test + Backend | Valid and invalid gates implemented; not run |
| Atomic registration + durable enqueue | `evidence-register-and-enqueue-commit-atomically`; `evidence-enqueue-failure-rolls-back-registration` | DB/API | Fixed evidence rows and forced after-document/before-enqueue fault | Registration and enqueue commit together or both roll back; retry creates one document/job | DB transcript and API trace | Backend + Test | Prepared vectors; adapter blocked on final Backend API/RPC |
| Worker expired-before-reclaim fencing | `expired-owner-cannot-mutate-before-reclaim` | DB/worker | Fixed clock, lease owner A, expired lease | A heartbeat/defer/write/settle all denied before reclaim | DB transcript and worker log | Backend + Test | Prepared vector; adapter blocked |
| Worker A→B lease transfer | `worker-a-to-b-lease-transfer-fences-a` | DB/worker | Fixed lease sequence and generation tokens | B reclaims once; every later A mutation denied | DB transcript and worker logs | Backend + Test | Prepared vector; adapter blocked |
| Successful evidence not downgraded | `ready-evidence-is-not-downgraded-by-pending-peer`; Slice 5 Ready assertion | Unit/DB + browser | Ready document plus `documents_not_ready` peer | Ready document remains successful | DB diff and UI trace | Backend + Test | Prepared vector; integrated adapter open |
| Expired collaborator | `expired collaborator cannot upload or process evidence` | Auth API/storage | User B expired grant, seeded evidence ID in the fixture case, valid PNG | Upload and process both return 403/404; no object/row/job created | Auth trace plus storage/DB diff | Test + Backend | API plus evidence-row/storage-object denial assertions implemented; job adapter open |
| Report/job status | State-family UI checks; Slice 5 processing; future job API poll | Browser/API | Running, drafting, ready, failed cases | Canonical status maps to expected UI and terminal state | Trace, screenshot, job response | Test + Backend | UI fixtures implemented; final worker poll open |
| Free draft | State-family `free draft ready` and free-case fixture | Browser | S$0 free case | Draft remains available without checkout | Screenshot and evidence JSON | Test + Frontend | State fixture implemented; full generation live flow open |
| Enabled S$18/S$188 checkout | Commercial authority unit gate; two provider checkout tests | Unit + preview provider | Empty self-serve and Tier 2 cases | Only one-time keys create `cs_test_` sessions; Stripe-hosted UI shows SGD 18/188 and no monthly cadence | Unit output and encrypted Stripe-hosted trace | Test + Payments | Implemented; not run |
| S$0 authority | `S$0 remains a non-checkout path` | Unit/browser | `free`, `tier0_free`, free fixture case | No checkout product or provider redirect | Unit output and UI trace | Test + Product | Unit implemented; UI assertion open |
| Disabled consultation/subscription/regeneration | Commercial authority unit gate; disabled provider-key test | Unit + API | Consultation, subscription, S$8/S$12 keys | Fail closed before Stripe | Unit output and provider trace | Test + Product | Implemented; current baseline consultation drift is preparation-only, not a test-branch defect or candidate evidence |
| Checkout cancellation/resumption | Fixture `cancelResumeCaseId` | Provider browser | Stripe test-mode cancellation | Return to same case; entitlement unchanged; retry available | Stripe trace and case DB snapshot | Payments + Test | Fixture prepared; browser steps open |
| Delayed entitlement | `successful return waits for delayed entitlement` | Browser/API | Delayed session/case fixture | Pending UI until server entitlement arrives; no false failure | Trace and poll transcript | Test + Backend | Browser assertion drafted; provider transition open |
| Payment failure/action required | `payment failure keeps the user on the case and offers a safe retry` | Synthetic + provider browser | Decline/action-required test methods | Case data remains; safe retry; no false entitlement | Trace and ledger snapshot | Payments + Test | Synthetic failure drafted; provider action-required open |
| Block second checkout | Provider test plus four lifecycle vectors | API/DB | Paid, partially-refunded, refunded, disputed unfulfilled purchases | 409; no second reservation/session | Trace and ledger count | Backend + Payments + Test | API assertions and vectors implemented; not run |
| Concurrent reservation/completion | `concurrent checkout reservation has one winner`; completion vectors | API/DB | Two simultaneous requests/events | One reservation and one fulfilment transition | Trace and DB transcript | Backend + Payments + Test | Reservation API assertion prepared; completion adapter open |
| Webhook order/duplicates | `payment-lifecycle-permutations-are-monotonic` | Unit/DB/webhook | All 24 orders of completed/partially-refunded/refunded/disputed; 480 single-event replays across every sequence boundary | Idempotent deterministic terminal ledger | Unit output, webhook responses, ledger snapshot | Backend + Payments + Test | Exhaustive vectors prepared; adapter blocked |
| Refund/dispute non-destructive | Refund/dispute lifecycle vectors | Unit/DB | Fulfilled purchases then refund/dispute | Purchase history preserved; case/report/evidence not deleted or downgraded | Before/after DB snapshot | Backend + Payments + Test | Prepared vectors; adapter blocked |
| Invitation/share | Invalid invitation synthetic test; report/share fixture | Browser/API/SMTP | Invalid token; controlled recipient | Invalid token stays isolated; authorized share sends once | Trace and SMTP message ID | Test + Backend | Invalid flow implemented; happy path open |
| Privacy export | `data export contains only the authenticated owner data` | Auth API | User A and user B fixture IDs | Export contains A and excludes B | Encrypted auth artifacts | Test + Backend | Implemented; not run |
| Deletion request | `deletion request anonymizes only a disposable authenticated fixture` | Auth API/DB | Dedicated disposable Clerk user and case; pre-delete owner/export identity check | The fixture is proven disposable before mutation; only its cases are anonymized | Encrypted response and DB audit | Test + Backend | Identity preflight and deletion assertions implemented; not run |
| Canonical CTA/API authority | Commercial unit gate; CTA canonical-route test; public contract-surface assertions; strict API schemas | Unit + browser/API | Public home/pricing/how-it-works/product/marketplace pages; forbidden client fields | UI points at canonical pricing/how-it-works paths, retired consultation/subscription links are absent, and server derives owner/payment state | Unit output and trace | Architect + Frontend + Backend + Test | Expanded in public specs; API inventory remains open |
| `/pricing` and `/how-it-works` deduplication | Canonical route tests | Public browser/HTTP | Direct canonical requests and `/product` request | `/pricing`, `/how-it-works`, and `/product` return canonical 200 pages with no duplicate-route redirect loops; `/product` links to canonical pricing/how-it-works | Public trace and screenshots | Architect + Frontend + Test | Updated to current route contract; not run |
| All public/auth routes | Public route, auth routing, contract-surface, canonical, synthetic router and authenticated-state suites | Browser | Route inventory and state fixtures | No unexpected redirect, client/hydration error, overflow, or contract-copy drift on public/auth surfaces | HTML report, screenshots, traces | Test + Frontend | Public suite expanded with FAQ/contact/resources/disabled-service contract assertions; authenticated new-case/signup remain gaps |
| 390/768/1440 + WebKit/mobile | Playwright project matrix | Browser | Four projects | Same contract at all target widths and engines | Per-project evidence records | Test | Implemented |
| Keyboard/focus/dialog/accessibility | Accessibility suite plus interactive-control inventory | Browser | Keyboard-only navigation, display/sensory settings dialog, reduced motion, contrast, public/router/authenticated-state controls | Logical focus; trapped modal; Escape restores trigger; readable contrast; no motion; every visible control is named | Trace and screenshots | Test + Frontend | Broad automated inventory implemented; manual semantic/accessibility review remains open |
| Visual baselines | `visual-baselines.spec.ts` | Screenshot | Public route × browser project | Baseline matches within threshold | Reviewed snapshots and diff artifacts | Test + Design | Test list implemented; baselines intentionally absent pending human review |
| Offline/reconnect/back/refresh | Router recovery tests | Synthetic browser | Offline browser state, expired session token, restored draft and catch-up fixtures | Offline blocks auth handoff while retaining draft; expired sessions are replaced deterministically; restored draft survives reload | Synthetic traces | Test + Frontend | Updated vectors implemented; authenticated realtime reconnect remains open |
| Migration deterministic preflight conflicts | `migrationPreflightCases` | Migration/DB | Duplicate mappings, orphan ownership, ambiguous historical rows | Fixed ordered conflict report; migration aborts without partial writes | Migration log and DB snapshot | Backend + Test | Prepared vectors; migration adapter blocked |
| Migration lint/apply | `migration-rls` workflow job | Local Supabase | Empty local database, full migration set | Reset/apply and lint succeed | CI log | Test + Backend | Workflow prepared; not run |
| Five external handshakes | Clerk claim, Supabase RLS, Stripe checkout, evidence worker/LLM Ready poll, SMTP acceptance | Preview provider | Controlled users/cases/files/recipient | Every handshake succeeds on its first attempt at the exact SHA; zero retries; any failure blocks promotion | Auth/preview evidence JSON, encrypted traces, provider IDs | Test + owning agents | Tests distributed across lanes; end-to-end delivery receipt and final worker adapter open |
| CI required gates | Harbor release workflow | GitHub Actions | PR head SHA | Install, static, migration/RLS, public, synthetic, preview, auth all required | Check runs and artifacts | Test + repository admin | Workflow prepared; branch-protection administration external |
